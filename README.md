# Bingo Battles

## How to run locally

1. Install dependencies.

```bash
cd Backend
npm install

cd ../Frontend
npm install
```

2. Start the backend.

```bash
npm run backend
```

3. In a new terminal, start the frontend.

```bash
npm run start
```

4. Open the website in your browser.

```text
http://localhost:5173
```